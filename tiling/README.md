# Directory contains scripts to tile raw tiffs, and raw shapefiles.

The tiffs and shapefiles are changed to pngs and GeoJSONs respectively, which can then be processed by our Mask R-CNN implementation.
